// Public, absolute base URL of the deployed site. Used for metadataBase,
// canonical/hreflang links, Open Graph URLs, and the sitemap. Defaults to the
// local dev origin.
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000';

export const SITE_NAME = 'DarSyria';
